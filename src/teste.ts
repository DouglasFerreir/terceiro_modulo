// smartphone = objeto
// 3 propriedades desse objeto

const smartphone {
    marca = Apple
    modelo: Iphone15
    internet5G: Sim
}
    // metodo com parametro

    mensagem (texto: string) {
     console.log mensagem("O(a) senhor(a) recebeu a seguinte mensagem:", mensagem)
}
   
//  metodo sem parametro

discagem() {
     console.log discagem("discando:", discagem)
}
